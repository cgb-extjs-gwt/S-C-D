
/**
 * Provides constraining behavior for a Ext.drag.Source.
 * 
 */
export const constraint = 'constraint';

/**
 * A drag proxy that creates a new element to follow the cursor.
 * 
 */
export const proxy = 'proxy';